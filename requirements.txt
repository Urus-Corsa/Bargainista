# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.19

# Pydantic & settings
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# Task queue
celery[redis]>=5.4.0
redis>=5.2.0

# AI / LLM orchestration
langchain>=0.3.0
langgraph>=0.2.0
anthropic>=0.43.0
langsmith>=0.2.0

# MCP (Model Context Protocol) — server and client
mcp>=1.27.0

# HTTP client (ingestion + health checks)
httpx>=0.28.0

# Image handling
pillow>=11.1.0

# PDF text extraction (pdfplumber is pure Python — no system packages required)
pdfplumber>=0.11.0

# Environment & utilities
python-dotenv>=1.1.0
